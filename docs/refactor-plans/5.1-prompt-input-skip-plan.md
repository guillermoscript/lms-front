# 5.1 prompt-input.tsx — SKIPPED

## Reason
`components/ai-elements/prompt-input.tsx` (1,263 lines) is **third-party generated code** from the `ai-elements` library, installed via `npx ai-elements@latest add prompt-input`.

Evidence:
- `.agents/skills/ai-elements/references/prompt-input.md` documents installation as `npx ai-elements@latest add prompt-input`
- The entire `components/ai-elements/` directory (48 files) follows this pattern
- No `ai-elements` package in `package.json` — files are scaffolded/copied into the project

## Assessment
The component already uses compound component architecture (PromptInput, PromptInputTextarea, PromptInputHeader, PromptInputFooter, etc.) with shared context. Decomposing it would create maintenance burden when updating the library.

## Decision
Do not modify. If the team forks and maintains this code independently, revisit.
