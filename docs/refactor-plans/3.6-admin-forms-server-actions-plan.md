# 3.6 Product/Plan Forms — Extract Mutations to Server Actions

## Status: SKIPPED (already done)

## Assessment
Both `ProductForm` and `PlanForm` already use server actions for mutations:
- `ProductForm` imports `createProduct`, `updateProduct` from `app/actions/admin/products.ts`
- `PlanForm` imports `createPlan`, `updatePlan` from `app/actions/admin/plans.ts`

No admin form component uses `createClient()` from `@/lib/supabase/client` for mutations. The only component that *was* using the client was `CourseSelector` (for reads), which was fixed in subtask 3.1.

A grep for `supabase/client` in `components/admin/` confirms zero remaining client-side Supabase usage in admin components.
