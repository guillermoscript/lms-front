# 1.7 Certificate Template Form — Combined Refactor Plan

## Current State
- **File:** `components/teacher/certificate-template-form.tsx` (692 lines)
- **Mutations:** 1 client-side upsert to `certificate_templates` table
- **Uploads:** 1 image upload (already uses server action `uploadCertificateAsset`)
- **Used in:** `app/[locale]/dashboard/teacher/courses/[courseId]/certificates/settings/page.tsx` (dynamic import)

## Mutations Found

| # | Operation | Table | Type | Line |
|---|-----------|-------|------|------|
| 1 | `handleSubmit` — upsert template | `certificate_templates` | upsert (onConflict: course_id,tenant_id) | 72-119 |

The image upload already calls `uploadCertificateAsset` server action — no change needed there.

## Server Action Plan

### File: `app/actions/teacher/certificates.ts`

**`upsertCertificateTemplate(courseId, data)`**
- Auth: `requireTeacherOrAdmin()` + `verifyCourseOwnership(ctx, courseId)`
- Tenant filter: `tenant_id` from auth context (not from client)
- Upsert with `onConflict: 'course_id,tenant_id'`
- Revalidate path

## Component Split Plan

### Directory: `components/teacher/certificate-template-form/`

| File | Responsibility | Lines (est) |
|------|---------------|-------------|
| `certificate-template-context.tsx` | Context provider, form state, actions | ~120 |
| `certificate-info-section.tsx` | Name, description, criteria fields | ~60 |
| `issuer-section.tsx` | Issuer name/url + logo upload | ~80 |
| `signature-section.tsx` | Signature name/title + image upload | ~80 |
| `completion-criteria-section.tsx` | Sliders + toggles for completion requirements | ~70 |
| `expiration-section.tsx` | Expiration toggle + days input | ~60 |
| `design-section.tsx` | Color presets, custom colors, QR toggle | ~100 |
| `certificate-template-form.tsx` | Shell — composes sections + preview | ~50 |
| `index.tsx` | Barrel export | ~3 |

**Total: ~623 lines across 9 files** (vs 692 monolith)

## Import Path Preservation

The page uses:
```ts
dynamic(() => import('@/components/teacher/certificate-template-form').then(m => m.CertificateTemplateForm))
```

The barrel `index.tsx` re-exports `CertificateTemplateForm` — no consumer changes needed.

## Props Changes

The `tenantId` prop is no longer needed — the server action gets it from auth context. The component interface becomes:

```ts
interface CertificateTemplateFormProps {
  courseId: number
  initialData?: any
}
```

The page passes `tenantId` but it will be ignored (or we can remove it from the page too).
