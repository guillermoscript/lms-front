# 5.3 AI elements — lazy load heavy components — SKIPPED

## Already Done (Phase 0.2)
The three heaviest AI consumer components are already dynamically imported:

1. **AristotlePanel** — `app/[locale]/dashboard/student/courses/[courseId]/layout.tsx` line 7
2. **AristotleStudySection** — `app/[locale]/dashboard/student/courses/[courseId]/page.tsx` line 21
3. **LessonAIChat** — `app/[locale]/dashboard/student/courses/[courseId]/lessons/[lessonId]/page.tsx` line 13

## Remaining static imports (low priority)
- `ExerciseChat` — statically imported in exercise page, but rendered on nearly every exercise type (essay uses it directly). Dynamic import saves little since it's always needed.
- `AIPreviewModal` — imported in teacher subcomponents (exercise-ai-config-step, lesson-ai-task-step), not page-level. These are already behind teacher dashboard routes which are code-split by route.

## Decision
Major AI components already covered. Remaining ones have low bundle impact or are always rendered on their pages.
