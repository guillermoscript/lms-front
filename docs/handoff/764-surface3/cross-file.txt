T1-block-editor: components/teacher/block-editor/sortable-block.tsx — sortable-block.tsx imports BLOCK_ICONS (line 22, used line 66) and paints the 4px per-block accent strip with iconData?.bg and the type-label icon with iconData?.color. It is already clean (absent from the baseline) and its fallbacks are tokens, but after the BLOCK_ICONS collapse every strip becomes bg-brand-tint, which on the no-kit light default is oklch(0.96 0.02 223) on a bg-card of oklch(1 0 0) — almost invisible as a 4px sliver. Recommend its owner change the strip to bg-primary/40 or drop it. Also: do NOT simplify the BLOCK_ICONS record type, or this file stops compiling.
T1-block-editor: components/lesson/code-block.tsx — Learner code renderer, swept in #770 and deliberately left at [9, 2] with the same bg-[#0d1117] / bg-[#161b22] / gray toolbar chrome. It is the precedent judgment call T1-block-editor-2 option A leans on; if option B is chosen instead, the two files disagree about the same panel and this one should be revisited in a later surface.
T1-block-editor: components/lesson/ordering.tsx — The learner twin uses bg-muted text-muted-foreground (4.39:1) for its step-number pill, where plan rule E-3 proposes bg-muted text-foreground for the editor's. Cosmetic divergence only — flagged so the orchestrator can align them if it prefers.
T1-block-editor: components/ui/* — Every shared UI primitive this group uses — popover, select, input, textarea, button, switch, label — is absent from the palette baseline, so no primitive needs escalating.
T1-block-editor: components/lesson/matching-pairs.tsx — PAIR_COLORS (8 hardcoded palette pairs, 32 of the file's baseline count) is a legitimate content keep — connection identity, not chrome — but ships with no justification comment, unlike the convention surface 3 is applying. Minor, and outside this group.
T2-grading: components/teacher/course-students-table.tsx — components/teacher/course-students-table.tsx (NOT in my list) is already [0,0] and consumes EngagementBadge/ProgressCell from student-progress-cells.tsx — so the STATUS_STYLE edit is the last palette colour in that whole Students-tab path. No edit needed there, but its before/after screenshots are the ones that show my change.
T2-grading: components/ui/badge.tsx — Shared UI primitives used here are already token-clean: components/ui/badge.tsx (destructive variant is bg-destructive/10 text-destructive, outline is border-border/bg-input) and components/ui/card|sheet|table. Nothing to report upstream.
T2-grading: components/teacher/issue-certificate-button.tsx — components/teacher/issue-certificate-button.tsx is rendered inside student-progress-sheet.tsx and carries 6 palette classes in the baseline — another group owns it; my sheet edits do not touch it.
T2-grading: tests/unit/palette-class-guard.test.ts — The guard's palette regex cannot match side-specific colour utilities (`border-l-green-500`, `border-t-amber-400`, …) because the side letter sits between the prefix and the hue. submission-review.tsx:211 is one such pair, invisible to both counts. Worth telling whoever owns the guard — other groups may have the same blind spot.
T3-routes: components/teacher/certificate-preview.tsx — Imported by the certificates page and still on the baseline at 14 palette + 1 raw; it renders the certificate design preview, so its colours and the page's swatches must stay in agreement
T3-routes: components/teacher/preview-banner.tsx — Imported by the course preview page (<PreviewBanner>), 6 palette on the baseline — the amber 'you are previewing' band sits directly above the page I am sweeping
T3-routes: components/teacher/issue-certificate-button.tsx — Imported by the certificates page, 6 palette on the baseline
T3-routes: components/shared/onboarding-checklist.tsx — Imported by the teacher dashboard (<OnboardingChecklist>), 9 palette on the baseline
T3-routes: app/[locale]/dashboard/student/courses/[courseId]/page.tsx — Line 219 carries the same dead shadow-[0_0_10px_rgba(var(--primary),0.5)] as my preview page — swept by #770 and left in place; see judgment call T3-routes-7 before diverging from it
T3-routes: lib/certificates/default-design.ts — Exports DEFAULT_CERTIFICATE_DESIGN ('#3B82F6' / '#1E40AF' / show_qr_code), the constant the certificates page should import instead of re-inlining those hexes four times; read-only for this sweep
T3-routes: components/teacher/revenue-chart.tsx, components/teacher/transaction-list.tsx, components/teacher/payout-history.tsx, components/teacher/course-students-table.tsx — All four are imported by my files and are already clean — absent from the baseline, no hex literals, no Recharts fill/stroke/colors props. Listed so nobody re-checks them
T4-versioning: components/teacher/lesson-editor/markdown-field.tsx — components/teacher/lesson-editor/markdown-field.tsx:52 defines `surface: 'bg-[#1e1e2e]'` as a named constant and components/teacher/exercise-builder/exercise-ai-config-step.tsx:85 uses the same literal; they are the same visual family as my two dark panels, so T4-versioning-1/2 must be decided the same way for them or the editor and its preview stop matching. Baselines: markdown-field [3,16], exercise-ai-config-step [13,7].
T4-versioning: app/[locale]/dashboard/student/courses/[courseId]/lessons/[lessonId]/lesson-content.tsx — app/[locale]/dashboard/student/courses/[courseId]/lessons/[lessonId]/lesson-content.tsx kept #1e1e2e and a white-alpha rule through the merged surface-1 sweep (baseline [0,3]) — but there it backs a syntax highlighter whose token colours are that theme. My two panels render plain unhighlighted text, which is the crux of T4-versioning-1/2; cite this file if the keep branch is chosen.
T4-versioning: components/ui/dialog.tsx — components/ui/dialog.tsx:34 and components/ui/alert-dialog.tsx:33 both use a black/80 modal scrim (baseline [0,1] each). Every file in this group renders inside one. A scrim over content is a justified keep — flagged only so nobody counts it against these six files.
T4-versioning: components/ui/badge.tsx — components/ui/badge.tsx is fully tokenised, so the Badge variant="outline"/"secondary" uses across four of my files need no companion change.
T4-versioning: components/ai-elements/suggestion.tsx — The @/components/ai-elements pieces ai-preview-modal.tsx imports (Conversation, Message, PromptInput*, Suggestion) are absent from the baseline, i.e. already clean; the dirty ai-elements files (commit, terminal, schema-display, test-results, tool) are not reached from this group.
T5-builders: components/teacher/improved-template-selector.tsx — improved-template-selector.tsx carries its own copies of the code-editor skin (bg-[#1e1e2e], text-[#cdd6f4]) at L324-325 and L340-341 for its template preview panes, baseline [8, 6]. It is rendered inline inside two of my files (exercise-ai-config-step L9, lesson-ai-task-step L14), so whatever T5-builders-1 decides must be applied here too or the same screen shows one dark prose panel next to one light one.
T5-builders: components/teacher/ai-preview-modal.tsx — ai-preview-modal.tsx has 8 palette classes (baseline [8, 0]) that render inside my screens; it is imported by exercise-ai-config-step L10 and lesson-ai-task-step L15.
T5-builders: components/teacher/version-history-sheet.tsx — version-history-sheet.tsx (baseline [8, 0]) is imported by exercise-builder-toolbar L7 and lesson-editor-header L17; its own child version-diff-panel.tsx (baseline [31, 7]) holds a third copy of the #1e1e2e/#cdd6f4 code skin plus a red/green diff palette that is arguably content.
T5-builders: components/teacher/lesson-editor/lesson-content-step.tsx — lesson-content-step.tsx is the other consumer of MarkdownField and the one that actually passes variant="terminal"; it is clean (absent from the baseline) and needs no edit, but any change to the TERMINAL skin lands on that screen and should be QA'd there.
T5-builders: app/[locale]/dashboard/student/courses/[courseId]/lessons/[lessonId]/lesson-content.tsx — #770 kept prose-pre:bg-[#1e1e2e] in the student lesson-content prose class string, which is the precedent that justifies keeping the same surface for the MDX editor in markdown-field.tsx. Not an edit - cited as the reason for the keep.
T5-builders: components/ui/ — Every components/ui/* primitive used by this group (Badge, Button, Input, Textarea, Label, Separator, Checkbox, Dialog, Select) is absent from the palette baseline, i.e. already clean - no shared-component blockers for this group.
T6-certificates: components/teacher/version-history-sheet.tsx (and 5 others) — The decorative coloured icon chip (w-8/9 h-8/9 rounded-lg bg-<hue>-500/10 + text-<hue>-600 dark:text-<hue>-400) is not local to this group: components/teacher/version-history-sheet.tsx:191 (amber), components/teacher/improved-template-selector.tsx:149 (violet), components/teacher/ai-preview-modal.tsx:98 (violet), components/teacher/lesson-editor/lesson-ai-task-step.tsx:51,81, components/onboarding/onboarding-wizard.tsx:189,343, components/tenant/create-school-flow.tsx:458. All should take the same RULE S1 mapping so the surface converges.
T6-certificates: app/[locale]/verify/[code]/page.tsx — Precedent to match: app/[locale]/verify/[code]/page.tsx (swept in surface 2) renders the same credential with bg-card / border-border / text-foreground, keeps the template's primary_color inline, and carries exactly one justified raw keep for initials over the template colour — including the comment wording convention this group reuses.
T6-certificates: components/teacher/certificate-template-form/certificate-template-context.tsx — COLOR_PRESETS (8 hardcoded hex pairs) lives in certificate-template-context.tsx, which the guard scores 0/0 because they are data, not classes. They are content — the ink palette offered to the teacher — and must NOT be tokenised; same for lib/certificates/default-design.ts and the document palette inside lib/certificate-generator.ts.
T6-certificates: app/[locale]/dashboard/teacher/courses/[courseId]/certificates/page.tsx — app/[locale]/dashboard/teacher/courses/[courseId]/certificates/page.tsx (baseline [11,0]) hosts CertificatePreview at line 178 on a bare page background with no wrapper card. Whoever sweeps it should not wrap the preview in a themed panel before T6-certificates-1 is decided, or the paper/panel decision gets made twice.
T6-certificates: components/shared/upgrade-nudge.tsx — components/shared/upgrade-nudge.tsx, imported by design-section for the basic-tier lock, is already token-clean (0/0) — no cross-file work needed.
A1-money: components/ui/badge.tsx — components/ui/badge.tsx has no success/warning variant, so all four status maps in this group hand-roll 'bg-success/10 text-success border-success/30'. The file is clean in the baseline (no colour debt), but a success/warning variant would reduce every admin status map to one word — epic follow-up, not this sweep.
A1-money: components/ui/card.tsx — Every shared component these five pages import (card.tsx, table.tsx, avatar.tsx, button.tsx, components/admin/admin-breadcrumb.tsx) is clean in palette-class-baseline.json — no colour reaches these pages from outside their own source.
A1-money: app/[locale]/dashboard/student/billing/page.tsx — Convergence anchor for sibling A-group agents: the exact status strings recommended here are copied from the merged surface-1 sweep at app/[locale]/dashboard/student/billing/page.tsx:105-111, and the solid warning fill from components/exercises/exam-card.tsx:126.
A1-money: components/ui/badge.tsx — Badge instances in payouts/invoices/transactions pass a colour className with no variant prop; Badge composes through cn()/tailwind-merge so bg-success/10 etc. override the default variant's bg-primary. Do not add variant props — the sweep is colour-only and the current merge resolves correctly.
A2-commerce: components/ui/badge.tsx — components/ui/badge.tsx has no `success` variant (only default/secondary/destructive/outline/ghost/link), so `bg-success/10 text-success border-success/30` gets hand-written in three files here and in several teacher files per sibling plans. Durable fix: add a `success` entry to badgeVariants alongside `destructive` (which is already a token tint at /10). Raise as a follow-up after all surface-3 groups land.
A2-commerce: components/admin/subscription-actions.tsx — components/admin/subscription-actions.tsx (rendered by subscriptions/page.tsx:338) still has 2 palette classes: `text-orange-600` (line 128) and `text-green-600` (line 138) on dropdown-item icons. Baseline [2, 0]. Same shape as judgment call A2-commerce-2 — map to text-warning / text-success.
A2-commerce: components/admin/courses-table.tsx — Clean, nothing needed: courses-table.tsx, categories-table.tsx, product-actions.tsx, admin-breadcrumb.tsx, confirm-dialog.tsx are all absent from the baseline and grep clean. No shared component blocks this group's sweep.
A3-payments: app/[locale]/dashboard/admin/subscriptions/page.tsx — app/[locale]/dashboard/admin/subscriptions/page.tsx (baseline [20, 0]) line 299 styles the active-subscription badge `bg-emerald-100 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-400`; it renders <SubscriptionActions> in the same row, so my text-success/text-warning edits land next to it. Its owner should use bg-success/10 text-success.
A3-payments: app/[locale]/dashboard/admin/payment-requests/[requestId]/page.tsx (and ~15 sibling admin routes) — The admin page-header band `<header className="border-b bg-card">` is repeated across ~15 admin routes including my detail page (line 136). On the no-kit default --card equals --background, so the band vanishes in light mode. Token-based already, guard-silent; it is one surface-wide decision (bg-muted), not eight independent ones, so I planned NOT to touch my copy.
A3-payments: components/public/checkout-form.tsx — components/public/checkout-form.tsx (surface 2, merged, baseline [0, 2]) keeps the identical QR light plate twice with a written justification comment — that is the precedent for my one keep, and the new comment should be worded to match it so the two read as one rule.
A3-payments: components/ui/badge.tsx — Shared components consumed by this group are all token-clean with no baseline entries: components/ui/badge.tsx, components/ui/button.tsx, components/ui/dropdown-menu.tsx, components/shared/proof-upload.tsx, lib/payments/provider-presentation.ts. One mechanic matters for execution: Badge variant='outline' supplies `border-border bg-input/20 text-foreground` and cn() is tailwind-merge, so every tinted status pill needs an explicit border-transparent or the neutral border muddies it.
A3-payments: app/[locale]/dashboard/admin/payment-requests/page.tsx — app/[locale]/dashboard/admin/payment-requests/page.tsx (clean, no baseline entry) is the parent of my table; its four stat cards carry no status colour at all. If A3-payments-1 lands, echoing the pill colours there is a design follow-up, not a sweep item.
A4-billing-users: components/shared/limit-reached-banner.tsx — components/shared/limit-reached-banner.tsx [5, 0] is rendered twice by billing-overview.tsx (courses + students). Its warning branch at L41 is 'border-yellow-500/50 bg-yellow-50 text-yellow-800 dark:bg-yellow-950/20 dark:text-yellow-200', sitting directly under the usage-meter bars this plan tokenises — after the sweep the meter says text-warning and the banner an inch below still says yellow-800. Its isAtLimit branch is already border-destructive/50 bg-destructive/10 text-destructive. One-line fix: 'border-warning/30 bg-warning/10 text-warning'.
A4-billing-users: components/shared/onboarding-checklist.tsx — components/shared/onboarding-checklist.tsx [9, 0] is rendered by app/[locale]/dashboard/admin/page.tsx:255 and is the first thing an admin sees. Its milestone card (L209-220) is emerald throughout: border-emerald-500/25, bg-emerald-500/[0.06], bg-emerald-500/15 text-emerald-700 dark:text-emerald-300, text-emerald-950 dark:text-emerald-50, text-emerald-900/70 dark:text-emerald-100/70. Maps to border-success/30 bg-success/10 for the card, bg-success/15 text-success for the disc, and text-foreground / text-muted-foreground for the two copy lines (the emerald-950/emerald-50 pair is doing 'strong text', not 'success').
A4-billing-users: components/ui/badge.tsx — components/ui/badge.tsx is palette-clean but its `outline` variant is a trap for this whole sweep: the variant string contains dark:bg-input/30, and cn() (twMerge, lib/utils.ts) does not dedupe across the dark: modifier — so any caller passing a base-class status tint (bg-success/10) loses it in dark mode. That is why today's code carries dark:bg-green-950. No edit proposed to the component; the rule for every surface-3 group is: when overriding a Badge fill with a status tint, drop variant="outline" (default/secondary have no dark: classes and dedupe cleanly).
A5-landing: components/ui/dialog.tsx:34 — Dialog overlay is bg-black/80 (baseline [0,1]); TemplatePicker renders through it, so the admin builder's modal scrim is a raw colour. Plausible keep, but needs a justifying comment and belongs to the components/ui owner.
A5-landing: components/ui/alert-dialog.tsx:33 — Identical bg-black/80 overlay (baseline [0,1]); the delete confirmation in landing-pages-client renders through it.
A5-landing: components/ui/badge.tsx — Checked because option A leans on it: the outline variant is border-border bg-input/20 text-foreground dark:bg-input/30 - fully tokenised, no finding, so the neutral-badge plan is safe.
A5-landing: components/admin/landing-page/puck-editor.tsx — Dynamically imported by landing-pages-client; absent from the palette baseline (0/0). Nothing to do - noted so nobody re-checks it.
O1-wizard: components/tenant/create-school-flow.tsx — `components/tenant/create-school-flow.tsx` (baseline [69, 11]) uses the identical dark-zinc wizard idiom — step dots, zinc cards, blue CTAs. It is the screen immediately before onboarding, so whatever O1-wizard-2 decides must apply there too or the flow splits visually mid-signup.
O1-wizard: app/[locale]/create-school/page.tsx — `app/[locale]/create-school/page.tsx` (baseline [0, 1]) wraps that flow in `bg-[#0A0A0A]` — the raw-hex twin of the onboarding layout's zinc gradient. Same decision, same sweep.
O1-wizard: components/theme-kit/theme-kit-picker.tsx — `components/theme-kit/theme-kit-picker.tsx` is absent from the baseline (fully token-styled). No edit needed — it is only relevant because the wizard wraps it in a `dark text-foreground` div to compensate for the wizard's own dark card; that wrapper is deleted in my plan.
O1-wizard: components/theme-kit/theme-kit-preview.tsx — `components/theme-kit/theme-kit-preview.tsx:27` derives preview mode from next-themes' `resolvedTheme`, not from a DOM `dark` class, so deleting the wizard's `dark` wrapper does NOT change what the phone/school previews render. Confirms rule 12 is safe.
O1-wizard: app/[locale]/dashboard/student/page.tsx — `components/shared/onboarding-checklist.tsx` is rendered by the admin, teacher AND student dashboards; the student dashboard was already swept in surface 1, so sweeping this component also closes the last palette block on that learner page.
O1-wizard: components/ui/card.tsx — `components/ui/{card,button,input,label,textarea}.tsx` are clean and fully token-based (Card = bg-card + ring-1 ring-foreground/10; Button default = bg-primary text-primary-foreground; Input = bg-input/20 border-input). Every 'delete the className' rule in the plan depends on that and requires no change under components/ui.
O2-create-school: components/onboarding/onboarding-wizard.tsx — components/onboarding/onboarding-wizard.tsx carries the same dot stepper (its lines 149-162, bg-blue-500 reached / bg-zinc-700 upcoming) and the same emerald icon tile (bg-emerald-500/10 + border border-emerald-500/20 at lines 343 and 407). O1-wizard's plan maps those to bg-primary / bg-border and bg-success/10 + border-success/30 + text-success, which agrees with my rules 7, 12 and 14 except for the green 'done' state (O2-create-school-1) and the chip border opacity (O2-create-school-4).
O2-create-school: components/ui/card.tsx — Every shared primitive this group imports is already clean — components/ui/card.tsx, button.tsx, input.tsx, label.tsx, input-group.tsx and sonner are all absent from palette-class-baseline.json and read as fully tokenised. No edit outside my file list is implied; the plan relies on their defaults (bg-card + ring-1 ring-foreground/10, bg-primary text-primary-foreground, border-input bg-input/20, ghost's hover:text-foreground).
O2-create-school: app/[locale]/join-school/page.tsx — app/[locale]/join-school/page.tsx is already 0/0 (swept in #771) and is the nearest sibling screen; rule 14's bg-success/10 + text-success is copied from its member card so the two tenant-entry screens read the same.
O2-create-school: app/[locale]/auth/login/page.tsx — app/[locale]/auth/* shares the dark-card-on-dark-page language with create-school-flow.tsx, which links to /auth/login from two places (lines 405 and 431). If auth is not assigned to any surface-3 group, the swept create-school will hand the user across a light-to-dark seam.

--- BUGS ---
low: components/teacher/block-editor/editors/fill-in-the-blank-block.tsx — Line 53 hardcodes Spanish: {segment.type === 'blank' ? 'Blanco' : 'Texto'} in a file that otherwise uses useTranslations, so an English-locale teacher sees Spanish chips. Belongs to the #678/#689 i18n sweep.
medium: components/teacher/block-editor/editors/checkpoint-block.tsx — handleToggleRequired / handleToggleAllowSkip (lines 112-136) ignore result.success === false, and the error state is only ever written by handleCreate — a rejected updateLessonCheckpoint surfaces nothing, so the toggle appears to do nothing and the teacher cannot tell a failed write from a no-op.
low: components/teacher/block-editor/editors/checkpoint-block.tsx — Line 209 <Select value=""> is pinned to the empty string while onValueChange writes elsewhere, so the trigger always shows the placeholder and re-picking the same checkpoint id after clearing may not re-fire the handler. Not verified in a browser.
low: components/teacher/submission-review.tsx — Line 211: an ungraded free-text question (is_correct null/false before grading) renders the red 'incorrect' left border, so the card says 'wrong' before anyone graded it. Pre-existing; see judgment call T2-grading-5.
medium: components/teacher/submission-review.tsx — handleSaveReview writes exam_answers.feedback = teacher_notes and is_correct, but never persists points_earned / teacher_score_override — the points the teacher types in the override editor (line 321-327) are sent to onSave() only and are lost if the parent does not write them. Also `overrides` is keyed by ans.question_id while the UI reads overrides[q.id], so the map only lines up because question_id === q.id.
medium: components/teacher/submission-review.tsx — Line 173 reads submission.submitted_at, but per CLAUDE.md the exam_submissions order/date column is submission_date — likely renders 'Invalid Date' unless the parent aliases it. Same field read at exam-submissions-review.tsx:214.
low: components/teacher/exam-submissions-review.tsx — Line 199 renders submission.profiles?.email, but profiles has no email column (CLAUDE.md) — always blank.
low: components/teacher/submission-review.tsx — Line 202 sorts questions by a.sequence - b.sequence, but exam_questions has no `sequence` column (CLAUDE.md) — the sort is a no-op over undefined.
medium: app/[locale]/dashboard/teacher/courses/[courseId]/certificates/page.tsx — Lines 292 and 359 call t('manageCourse.studentList.unknownStudent') while t is already scoped to 'dashboard.teacher.manageCourse', so the lookup is dashboard.teacher.manageCourse.manageCourse.studentList.unknownStudent, which does not exist. Any student with a NULL full_name renders the raw key instead of 'Unknown Student'. Correct key is studentList.unknownStudent.
medium: app/[locale]/dashboard/teacher/courses/[courseId]/preview/page.tsx — Lines 37-41 filter the course query by .eq('author_id', userId), so a tenant admin who can open and edit the course (the course page allows admins per #690) gets notFound() from the Preview button on that same page. Same shape in .../exercises/page.tsx:34-42. Known #690 follow-up gap.
low: app/[locale]/dashboard/teacher/page.tsx — Lines 233-241: the 'quick actions' stat card draws a progress bar hardcoded at w-[65%] with no data source, captioned 'platform activity'. Cosmetic, but it reads as a real metric.
low: app/[locale]/dashboard/teacher/courses/[courseId]/preview/page.tsx — Line 136 (and the swept sibling app/[locale]/dashboard/student/courses/[courseId]/page.tsx:219) uses shadow-[0_0_10px_rgba(var(--primary),0.5)]. --primary holds an oklch() value, not an R,G,B triplet, so the declaration is invalid and the glow has never rendered.
medium: components/teacher/improved-template-selector.tsx — loadTemplates() does `const data = await res.json(); setTemplates(data)` with no ok/shape check (lines 59-66). A non-array error body makes `templates.length === 0` false and `templates.map` throw, blanking the dialog instead of showing the translated empty state.
medium: components/teacher/improved-template-selector.tsx — Hardcoded English in an otherwise next-intl file: 'Configure' (182), 'Preview' (194), `${n} variables to configure` (161), 'Ready to apply' (162), 'Click apply to use this template as-is.' (310).
low: components/teacher/ai-preview-modal.tsx — Line 107 uses bare `animate-pulse` on the preview-session dot; the repo convention elsewhere (e.g. version-history-sheet.tsx) guards these with motion-safe:, so this ignores prefers-reduced-motion.
low: components/teacher/version-diff-panel.tsx — The legend block is duplicated byte-for-byte at lines 403-415 (embedded) and 457-467 (standalone), so every colour edit has to be made twice and they can silently drift.
low: components/teacher/exercise-builder/exercise-ai-config-step.tsx — The system-prompt Textarea (L96-103) sits on a fixed dark #1e1e2e panel but its placeholder is placeholder:text-white/20, which is well under 3:1 against that surface in either app theme - the placeholder is effectively invisible. Rule 4 of the plan removes the whole panel, which incidentally fixes it, but flagging in case option B/C is chosen.
low: components/teacher/exam-builder/exam-question-ai-grading.tsx — bg-white/80 dark:bg-white/10 on the three inputs (L32, L44, L59) renders a near-invisible 10%-white wash in dark mode, so the controls have almost no surface separation from the surrounding bg-primary/5 panel today. The planned bg-background swap fixes it; noting that it is a real current defect, not only a theming one.
low: components/teacher/lesson-editor/markdown-field.tsx — The terminal header's line-count and filename use text-white/30 and text-white/40 on #1e1e2e, roughly 2.3:1 and 3.0:1 - below WCAG AA for the small text they carry. Inside the kept skin, so not a palette-sweep item, but worth a follow-up issue if the skin stays.
low: components/teacher/exercise-builder/exercise-details-step.tsx — The unselected difficulty chips use border-transparent while the selected one uses a coloured border, so the segmented control shifts no geometry but the whole group relies on fill contrast alone for the selected state; after the token swap the selected/unselected pair should be re-checked visually (the labels are always present, so this is polish, not an a11y failure).
low: components/teacher/certificate-template-form/design-section.tsx — The preset isActive check compares hex strings case-sensitively (design_settings.primary_color === preset.primary) while COLOR_PRESETS stores uppercase and <input type="color"> always emits lowercase (#3b82f6). Pick a colour with the picker, or reload a template saved from it, and no preset ever highlights even when the values match. hasCustomCertificateDesign in lib/certificates/default-design.ts already uppercases before comparing, so the two disagree.
low: components/teacher/issue-certificate-button.tsx — Four hover variants on the isSuccess button are dead: the same element sets disabled on the next line, so they can never render.
low: components/teacher/certificate-preview.tsx — The 'live preview' does not resemble the artefact it previews: the PDF (lib/certificate-generator.ts) is cream #fffef9 stock with a guilloche watermark, warm grey ink and a different layout, while the preview is plain white with cool greys. Cosmetic, but the teacher approves a design they will not receive.
low: components/teacher/certificate-template-form/design-section.tsx — The section header block (chip + heading) is duplicated verbatim in the basic-tier and custom-tier branches, so every chip change must be made twice in this one file.
medium: app/[locale]/dashboard/admin/transactions/page.tsx — Intl.NumberFormat hardcodes currency: 'USD' at lines 120, 139 and 210 while each transaction row carries its own currency, so a school selling in EUR reads dollar signs and the three stat cards sum mixed currencies into one figure. payouts/page.tsx solved exactly this with sumByCurrency/formatByCurrency (#531/#497).
medium: app/[locale]/dashboard/admin/transactions/page.tsx — Lines 61-65 read transactions with .select('*') and no pagination; the PostgREST 1000-row cap silently truncates the table and every stat derived from it (same class as #533/#548, which payouts fixed with fetchAllRows).
low: app/[locale]/dashboard/admin/transactions/page.tsx — Line 210 renders gross transaction.amount while the 'Total revenue' card is net of refunds (#547, line 82), so a partially refunded row never reconciles with the header total.
low: app/[locale]/dashboard/admin/invoices/page.tsx — Lines 66-71 read payment_requests unpaginated, so the invoice list silently truncates at the 1000-row cap.
medium: app/[locale]/dashboard/admin/subscriptions/page.tsx — The badge variant ternary is `isActive ? 'default' : isCancelled ? 'secondary' : 'destructive'`, so `renewed` and `past_due` — both LIVE statuses per CLAUDE.md — render painted like a failure. The stat counters at lines 114-119 also count only active/canceled/expired, so a renewed subscription is invisible in all four tiles.
low: app/[locale]/dashboard/admin/subscriptions/page.tsx — Lines 280-281 use `text-primary` on a `bg-primary/10` tint — exactly the combination the #764 convention says fails AA for light brands (brand-text exists for this). Not a palette class, so the guard cannot see it; deserves a separate repo-wide pass over `bg-primary/10 text-primary`.
low: app/[locale]/dashboard/admin/enrollments/page.tsx — Sub-AA muted text: `text-[11px] text-muted-foreground/70` at line 182 carries the student's email (real content), and `text-muted-foreground/60` appears at products/page.tsx:253 and plans/page.tsx:228. Dimming muted-foreground (already only ~4.4:1 on muted) below full opacity cannot clear AA at any size. Not guard-counted.
low: app/[locale]/dashboard/admin/plans/page.tsx — `IconArrowLeft` imported but never rendered (also in categories/page.tsx:7). Trivial dead import; the sweep does not touch these import blocks.
low: app/[locale]/dashboard/admin/enrollments/page.tsx — Line 236 closes with `</div >` (stray space before the bracket). Cosmetic.
low: app/[locale]/dashboard/admin/billing/checkout/[requestId]/solana-checkout-client.tsx — Line 117 falls back to `$${amountUsd.toFixed(2)}` when settlementLabel is null — hardcoded `$` and no locale formatting, on a screen otherwise careful about both (manual-transfer-form.tsx:36 was fixed for exactly this in #726).
low: app/[locale]/dashboard/admin/payment-requests/[requestId]/page.tsx — getStatusVariant (lines 98-113) is computed and passed to <Badge>, but every return value is immediately overridden by the className fill on line 155 — dead logic today, which is why the sweep can delete it outright.
low: components/admin/payment-request-dialog.tsx — Line 142 uses the native confirm() for the enroll confirmation while the rest of the group uses AlertDialog; inconsistent and unstyled, on the same money path.
medium: components/admin/usage-meter.tsx — L41-42 use `[&>div]:bg-destructive` / `[&>div]:bg-yellow-500` to colour the progress bar, but components/ui/progress.tsx renders Root > {children} + ProgressTrack > ProgressIndicator, so `> div` selects the TRACK, not the indicator. At >=80% usage the track turns yellow behind a bar that stays bg-primary — the opposite of the intent. Correct selector would be [&_[data-slot=progress-indicator]]:bg-warning. Pre-existing; the sweep should translate the colour only and leave the selector alone.
low: app/[locale]/dashboard/admin/users/[userId]/page.tsx — L226 'active' Badge has bg-green-50 text-green-700 border-green-200 with no dark: variants at all, so in dark mode it renders near-white text on a near-white fill. The token mapping fixes it incidentally.
medium: app/[locale]/dashboard/admin/billing/billing-dashboard-client.tsx — Hardcoded English strings survive in an otherwise next-intl file: L133/136 ('Renewal request submitted', 'Failed to submit renewal request'), L187/190 ('Proof uploaded successfully', 'Failed to upload proof'), L240 ('Subscription Renewal'), L244-245 (the whole period-ends sentence), L255 ('Renewal Pending' / 'Requesting...' / 'Renew via Bank Transfer'), L263 ('Redirecting...' / 'Switch to Stripe'), L354 ('Opening...' / 'View Invoices'). That is issue #678 territory, not #764 — do not fix in this sweep.
low: app/[locale]/dashboard/admin/notifications/page.tsx — L41-43 use `(n: any)` filters and the page imports IconPlus (L14) without using it — dead import. Cosmetic, out of scope.
low: components/admin/landing-page/template-picker.tsx — Untranslated English on an otherwise next-intl screen: raw {template.category} (line 242), '{count} sections' (248), aria-label="Templates" (214), aria-label="Back to page type selection" (120). #726 translated template names and page types but not these.
low: components/admin/landing-page/landing-pages-client.tsx — Untranslated English: table headers Page/URL/Status/Updated (297-301), timeAgo() strings 'just now'/'5m ago'/'3d ago' (88-98), raw {page.status} enum printed in the badge (360), generated names `${templateName} Page` (140) and `${page.name} (Copy)` (173).
low: components/admin/landing-page/template-picker.tsx — CATEGORY_COLORS has five unreachable keys (business, language-school, fitness, music, design) - no template in lib/puck/templates/index.ts uses them.
medium: components/admin/landing-page/template-picker.tsx — role="radio" buttons sit in a bare div grid with no role="radiogroup" owner and no arrow-key roving focus; separately role="listitem" is applied to <button> elements (214-221), overriding the implicit button role for assistive tech.
medium: components/admin/landing-page/landing-pages-client.tsx — <tr onClick> opens the editor with no keyboard equivalent on the row (306-310); the dropdown's Edit item is the only accessible path.
trivial: components/onboarding/onboarding-wizard.tsx — `<Card className="border-zinc-800 …">` (lines 169, 213, 271, 302, 405) sets a border COLOUR with no border-width utility, and Card itself uses `ring-1`, not `border` — the class has never rendered anything. Dead style, removed by the sweep anyway.
trivial: components/onboarding/onboarding-wizard.tsx — `backdrop-blur-sm` on the five Cards only did anything because the fill was `bg-zinc-900/50`; it is decorative chrome with no content behind it (the layout band is a flat gradient). Cost with no effect.
low: app/[locale]/onboarding/layout.tsx — The route's error boundary (`error.tsx` -> `components/shared/segment-error.tsx`) is fully token-styled, so today a light card renders on a zinc-950 gradient whenever onboarding errors. Pre-existing mismatch; fixed for free by the band swap.
trivial: components/onboarding/onboarding-wizard.tsx — `userId` is declared in `OnboardingWizardProps` and passed by the page but never destructured or used in the component. Unrelated to this sweep.
low: components/tenant/create-school-flow.tsx — Line 431 nests an interactive <Button> inside a <Link>, so a <button> sits inside an <a> — invalid HTML and the reason Playwright needs page.evaluate(() => btn.click()) on these. It is the documented base-ui pattern (no asChild prop), so it is intended, but rule 4 edits that exact line and the sweeping agent must resist fixing it.
medium: components/tenant/create-school-flow.tsx — Lines 383 and 501: the error paragraph has no role="alert" or aria-live, so a screen reader never hears a failed sign-up or a taken slug, while the stalled-Google hint 70 lines up does carry role="status". Rule 11 edits both error lines; adding the role would be a real a11y fix but is out of scope for a colour sweep.